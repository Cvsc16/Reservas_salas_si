<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Informe o ID do professor:" />

    <EditText
        android:id="@+id/professorIdEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="ID do Professor"
        android:inputType="number" />

    <EditText
        android:id="@+id/professorNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nome do Professor"
        android:visibility="gone" /> <!-- Escondido já que será preenchido automaticamente -->

    <EditText
        android:id="@+id/salaNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nome da Sala"
        android:visibility="gone" /> <!-- Escondido já que será preenchido automaticamente -->

</LinearLayout>
